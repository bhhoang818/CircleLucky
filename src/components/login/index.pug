section.citcle-lucky-wrapper 
    .wrapper-container.d-flex.align-items-center.justify-content-center
        .wrap-login 
            a(href="")
                img.logo(src="./images/logo.png", alt="") 
            .login-title 
                p.small-txt Chào mừng bạn đến với
                h1.big-txt Vòng quay may mắn
            form.wrap-form(action="") 
                .form-group 
                    label.label(for="") Tên truy cập
                    input.form-control(type="text" placeholder='Nhập tên user')
                .form-group 
                    label.label(for="") Mã Pin
                    input#core.form-control(type="password" placeholder='Nhập mã pin')
                    img.show-pass(src="./images/eye.svg", alt="")
                button.btn.btn-submit 
                    span Truy cập ngay
